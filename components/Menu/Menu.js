import styled from 'styled-components';
import { Box } from '../../styles';

const Menu = () => {

  return (
    <MenuWrapper>
      Menu
    </MenuWrapper>
  );
};

export default Menu;

const MenuWrapper = styled(Box)`

`;
